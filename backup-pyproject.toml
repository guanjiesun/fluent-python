[project]
name = "fluent-python"
version = "0.1.0"
description = "A journey to Fluent Python."
authors = [
    {name = "guanjie",email = "gjsun255@163.com"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = "3.10.11"
dependencies = [
]

[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
